# Coze API 配置
# 必填：Coze API Token（从 Coze 平台获取）
COZE_API_TOKEN=your_coze_api_token_here

# 必填：Coze Bot ID
COZE_BOT_ID=your_bot_id_here

# 可选：Coze API URL（默认：https://api.coze.cn/v3/chat）
# COZE_API_URL=https://api.coze.cn/v3/chat

# 可选：Coze Base URL（默认：https://api.coze.cn/v3）
# COZE_BASE_URL=https://api.coze.cn/v3

# Redis 配置
# 可选：Redis 连接 URL（默认：redis://localhost:6379/0）
# 如果 Redis 需要密码，格式：redis://:password@host:port/db
# REDIS_URL=redis://localhost:6379/0

# 认证配置
# 可选：是否启用认证（默认：true）
# ENABLE_AUTH=true

# 可选：HTTP 协议方案（默认：https://）
# HTTP_SCHEME=https://

# Host 配置（用于认证验证）
# 可选：Alchemy Host（默认：alchemy-studio.cn）
# ALCHEMY_HOST=alchemy-studio.cn

# 可选：Moicen Host（默认：moicen.com）
# MOICEN_HOST=moicen.com

# 可选：Huiwings Host（默认：huiwings.cn）
# HUIWINGS_HOST=huiwings.cn

# 可选：Local Host（默认：localhost）
# LOCAL_HOST=localhost

# 应用模式
# 可选：应用模式（remote 或 local，默认：remote）
# APP_MODE=remote

# 日志配置
# 可选：日志级别（默认：INFO）
# COZE_LOG_LEVEL=INFO

# 可选：日志格式（默认：json）
# COZE_LOG_FORMAT=json

# 请求配置
# 可选：API 请求超时时间（秒，默认：30）
# COZE_TIMEOUT=30

# 可选：最大重试次数（默认：3）
# COZE_MAX_RETRIES=3

# 可选：轮询间隔时间（秒，默认：1.0）
# COZE_POLL_INTERVAL=1.0

# Redis 会话配置
# 可选：会话过期时间（秒，默认：3600）
# COZE_SESSION_EXPIRE=3600

# 可选：结果过期时间（秒，默认：1800）
# COZE_RESULT_EXPIRE=1800

# 业务配置
# 可选：单条消息最大长度（字符，默认：4000）
# COZE_MAX_MESSAGE_LENGTH=4000

# 可选：每个用户最大并发会话数（默认：10）
# COZE_MAX_SESSIONS_PER_USER=10

# 数据保留策略配置
# 可选：最大保留活跃用户数（默认：1000）
# COZE_MAX_ACTIVE_USERS=1000

# 可选：最大总会话数（默认：5000）
# COZE_MAX_TOTAL_SESSIONS=5000

# 可选：用户不活动超时时间（秒，默认：7200）
# COZE_USER_INACTIVE_TIMEOUT=7200
